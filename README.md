# RESTful API
This API is created using Node.js and Express.

It allows users to view and add to a users list, visible from the webpage.
Additional functionalities include being able to update, delete, and view specific user information.

Postman was used to test the API endpoints.
